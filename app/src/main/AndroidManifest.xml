<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="io.github.marcinrogacki.goworld">
    <!--
    Changes information:

    android:allowBackup="false"
    Lint used to complain about missing backup info
    "AllowBackup/FullBackupContent Problems". Currently disabled since I don't
    know I need it.
    -->
    <application
        android:allowBackup="false" android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        >

    <!-- android:exported https://developer.android.com/guide/topics/manifest/activity-element#exported -->
    <activity android:name=".MainActivity" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
